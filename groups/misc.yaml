work:
  name: Work
  view: no
  entities:
    - binary_sensor.workday_sensor
    - sensor.home_to_work
    - sensor.work_to_home

datetime:
  name: Date and time
  view: no
  entities:
    - sensor.date
    - sensor.time
